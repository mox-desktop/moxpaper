[workspace]
members = ["ctl", "daemon", "common", "libmoxpaper"]
default-members = ["ctl", "daemon"]
resolver = "2"

[workspace.dependencies]
common = { path = "common" }
libmoxpaper = { path = "libmoxpaper" }

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "abort"
